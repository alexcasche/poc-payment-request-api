{
  "name": "poc-payments",
  "version": "0.0.0",
  "description": "A proof-of-concept webpage that demonstrates use of browser payment request API with Stripe",
  "scripts": {
    "dev": "vite --port $PORT",
    "build": "vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.1.5"
  },
  "dependencies": {
    "@stripe/stripe-js": "^1.13.2",
    "axios": "^0.21.1",
    "isomorphic-fetch": "^3.0.0",
    "stripe": "^8.145.0"
  }
}
